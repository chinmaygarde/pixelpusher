namespace pixel {
  enum CResult {
    kSuccess,
    kFailure,
  }

  //////////////////////////////////////////////////////////////////////////////
  /// Application
  //////////////////////////////////////////////////////////////////////////////
  struct CApplication {
    void* ffi_peer;
  }

  function ApplicationGetMain() -> CApplication*
  function ApplicationSetScene(CApplication* application, CScene* scene) -> CResult

  //////////////////////////////////////////////////////////////////////////////
  /// Scene
  //////////////////////////////////////////////////////////////////////////////
  struct CScene {
    void* ffi_peer;
  }

  function SceneCreate() -> CScene*
  function SceneCollect(CScene* scene)
}
